# Schemas

Schemas are YAML files that define required columns, recommended columns,
aliases, simple value constraints, and rule groups.

Bundled schemas:

- `generic-ngs`
- `bulk-rnaseq`

Show a schema:

```bash
metasheet-guard schema show bulk-rnaseq
```
