# Validator Rules

Implemented rule families include:

- table structure
- sample ID and metadata consistency
- FASTQ and file checks
- sample/run/lane relationships
- experimental design risks
- workflow export readiness

Rule codes are stable machine-readable identifiers such as
`REQUIRED_COLUMN_MISSING`, `FASTQ_PATH_NOT_FOUND`, and
`BATCH_CONDITION_CONFOUNDED`.
